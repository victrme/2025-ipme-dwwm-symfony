{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h1>Jeux de la catégorie {{ category.name }}</h1>

        {% for game in pagination %}
            {% include "template/_game_card.html.twig" with { game: game } %}
        {% endfor %}

        <div class="navigation">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>

{% endblock %}

{% block title %}
    {{ category.name }}
{% endblock %}

